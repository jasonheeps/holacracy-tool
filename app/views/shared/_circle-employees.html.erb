<section class="circle-employees-container <%= locals[:visibility_class] %>" id="<%= locals[:dataset_id] %>">
  <div class="cards-container">
    <% @employees.each do |e| %>
      <%= render 'shared/employee-card-simple', locals: { employee: e } %>
    <% end %>
  </div>
</section>
