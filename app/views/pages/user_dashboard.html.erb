<div class="user-dashboard-container page-container">
  <h1>user dashboard</h1>

  <%= link_to 'Schicht hinzufügen', new_shift_path %>

  <div class="shifts">
    <ul>
      <% @shifts.each do |s| %>
        <li><p> <%= "#{s.weekday}, #{s.time_start.strftime('%H:%M')}-#{s.time_end.strftime('%H:%M')}: #{s.role_filling_id}" %> </p></li>
      <% end %>
    </ul>
  </div>
</div>
